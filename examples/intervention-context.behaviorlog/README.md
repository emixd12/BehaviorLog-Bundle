# Example BehaviorLog Bundle

This synthetic bundle demonstrates the BehaviorLog core files. It is not real user data.

Read `manifest.json` first. The authoritative records are under `data/`. CSV files, when present, are optional views.
